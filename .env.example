# Backend (when connecting the frontend to the delivery API)
VITE_API_BASE_URL=http://localhost:3000/api

# Notification providers — configure on the backend, never expose secret keys to Vite
EMAIL_PROVIDER_API_KEY=
SMS_PROVIDER_API_KEY=
